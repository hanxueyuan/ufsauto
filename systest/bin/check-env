#!/usr/bin/env python3
"""check-env 小写命令入口"""
import sys
from pathlib import Path

# 添加父目录到路径
sys.path.insert(0, str(Path(__file__).parent.parent))

# 导入并执行 check_env
from bin.check_env import main
sys.exit(main())
